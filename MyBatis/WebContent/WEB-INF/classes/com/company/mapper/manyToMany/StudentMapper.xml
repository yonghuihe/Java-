<?xml version="1.0" encoding="UTF-8"?>	
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.manyToMany.StudentMapper">
	
	<insert id="add" parameterType="Student" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		insert into student (name) values (#{name})
	</insert>
	
	<select id="get" parameterType="Long" resultType="Student">
		select * from student where id = #{id}
	</select>
	
	<select id="getStuByTeaId" parameterType="Long" resultType="Student">
		select * from student where id in (
			select s_id from teacher_student where t_id = #{tid}
		)
	</select>
</mapper>